defmodule OverlappingModel do
  import Math
  alias Math
  # defstruct tile_size: nil, black: nill, tiles: [], propagator: %{}, periodic: nil


  def init(%{ tileperiodic: periodic }) do

  end

  

  # def Run(seed, limit, initial_state) do
    # logT = Math.log(initial_state.T)
    # range = 0 .. initial_state.T - 1
    # Enum.map(range, &)
  # end



end
